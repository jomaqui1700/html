import {NextResponse} from'next/server'
import {sql} from'../../../lib/db'
import {getSession} from'../../../lib/auth'
async function auth(){const s=await getSession();if(!s)return{error:NextResponse.json({error:'No autorizado'},{status:401})};return{session:s}}
function productsOf(b){const src=Array.isArray(b.products)?b.products:[];return src.map(p=>({inventory_id:Number(p.inventory_id||0),product_name:String(p.product_name||'').trim(),quantity:Number(p.quantity),unit:'sacos',price_per_sack:Number(p.price_per_sack||0)})).filter(p=>p.inventory_id||p.product_name||p.quantity||p.price_per_sack)}
function payload(b){const products=productsOf(b),total_cost=products.reduce((s,p)=>s+p.quantity*p.price_per_sack,0);return{application_date:String(b.application_date||'').slice(0,10),farm_id:Number(b.farm_id),abonada_number:Number(b.abonada_number),products,treated_area_ha:b.treated_area_ha===''||b.treated_area_ha==null?null:Number(b.treated_area_ha),total_cost,responsible:String(b.responsible||'').trim(),notes:String(b.notes||'').trim()}}
function validate(x){if(!x.application_date||!x.farm_id)return'Fecha y finca son requeridas.';if(![1,2,3].includes(x.abonada_number))return'Seleccione Abonada 1, 2 o 3.';if(!x.products.length)return'Debe registrar al menos un abono.';for(const p of x.products){if(!p.inventory_id)return'Seleccione cada abono desde Inventario Café.';if(!(p.quantity>0))return'La cantidad de sacos debe ser mayor que cero.';if(p.price_per_sack<0)return'El precio por saco no puede ser negativo.'}if(x.treated_area_ha!=null&&!(x.treated_area_ha>0))return'El área abonada debe ser mayor que cero.';return''}
function dbError(e,action='registrar'){console.error('coffee-abonadas',e?.code,e?.constraint,e?.message);if(e?.code==='23514')return NextResponse.json({error:'Un dato no cumple las reglas de la abonada. Revise sacos, precio y área.'},{status:400});if(e?.code==='23503')return NextResponse.json({error:'La finca, producto o usuario ya no existe. Actualice la página e intente de nuevo.'},{status:400});return NextResponse.json({error:`No se pudo ${action} la abonada. Código técnico: ${e?.code||'DB'}.`},{status:500})}
export async function GET(){const a=await auth();if(a.error)return a.error;try{const rows=await sql`SELECT r.*,COALESCE((SELECT json_agg(json_build_object('id',p.id,'product_name',p.product_name,'quantity',p.quantity,'unit',p.unit,'price_per_sack',p.price_per_sack,'subtotal',p.quantity*p.price_per_sack) ORDER BY p.id) FROM public.coffee_abonada_products p WHERE p.abonada_id=r.id),'[]'::json) AS product_list FROM public.coffee_abonada_report r ORDER BY r.application_date DESC,r.id DESC`;return NextResponse.json({abonadas:rows})}catch(e){return dbError(e,'cargar')}}
export async function POST(req){const a=await auth();if(a.error)return a.error;if(a.session.role!=='admin')return NextResponse.json({error:'Solo administrador.'},{status:403});try{const x=payload(await req.json()),e=validate(x);if(e)return NextResponse.json({error:e},{status:400});const productsJson=JSON.stringify(x.products);const rows=await sql`WITH p AS (SELECT * FROM jsonb_to_recordset(${productsJson}::jsonb) AS p(inventory_id bigint,product_name text,quantity numeric,unit text,price_per_sack numeric)),demand AS (SELECT inventory_id,SUM(quantity) quantity FROM p GROUP BY inventory_id),chk AS (SELECT COUNT(*)=(SELECT COUNT(*) FROM demand) AND COALESCE(BOOL_AND(i.id IS NOT NULL AND i.active AND i.category='Abono' AND i.unit='sacos' AND i.farm_id IS NULL AND i.current_stock>=d.quantity),false) AS ok FROM demand d LEFT JOIN public.coffee_inventory i ON i.id=d.inventory_id),ins AS (INSERT INTO public.coffee_abonadas(application_date,farm_id,method,abonada_number,treated_area_ha,total_cost,responsible,notes,created_by,updated_by) SELECT ${x.application_date},${x.farm_id},'Manual',${x.abonada_number},${x.treated_area_ha},${x.total_cost},${x.responsible||null},${x.notes||null},${a.session.id},${a.session.id} FROM chk WHERE ok RETURNING id),prod AS (INSERT INTO public.coffee_abonada_products(abonada_id,product_name,quantity,unit,price_per_sack) SELECT ins.id,p.product_name,p.quantity,'sacos',p.price_per_sack FROM ins CROSS JOIN p RETURNING abonada_id),mov AS (INSERT INTO public.coffee_inventory_movements(inventory_id,movement_date,movement_type,quantity,unit_cost,farm_id,reference_type,reference_id,notes,created_by) SELECT d.inventory_id,${x.application_date},'salida',d.quantity,i.unit_cost,${x.farm_id},'abonada',ins.id,${`Abonada ${x.abonada_number}`},${a.session.id} FROM demand d JOIN public.coffee_inventory i ON i.id=d.inventory_id CROSS JOIN ins RETURNING id) SELECT (SELECT id FROM ins) id,(SELECT ok FROM chk) inventory_ok`;if(!rows[0]?.inventory_ok||!rows[0]?.id)return NextResponse.json({error:'No se pudo descontar el inventario general. Actualice la página y verifique cantidad disponible.'},{status:400});return NextResponse.json({ok:true,id:rows[0].id,total_cost:x.total_cost})}catch(e){return dbError(e,'registrar')}}
export async function PATCH(req){const a=await auth();if(a.error)return a.error;if(a.session.role!=='admin')return NextResponse.json({error:'Solo administrador.'},{status:403});try{const b=await req.json(),id=Number(b.id),x=payload(b),e=validate(x);if(!id)return NextResponse.json({error:'Registro requerido.'},{status:400});if(e)return NextResponse.json({error:e},{status:400});const productsJson=JSON.stringify(x.products);const rows=await sql`WITH p AS (SELECT * FROM jsonb_to_recordset(${productsJson}::jsonb) AS p(inventory_id bigint,product_name text,quantity numeric,unit text,price_per_sack numeric)),newd AS (SELECT inventory_id,SUM(quantity) quantity FROM p GROUP BY inventory_id),oldd AS (SELECT inventory_id,SUM(quantity) quantity FROM public.coffee_inventory_movements WHERE reference_type='abonada' AND reference_id=${id} AND movement_type='salida' GROUP BY inventory_id),balance AS (SELECT COALESCE(n.inventory_id,o.inventory_id) inventory_id,COALESCE(n.quantity,0) new_qty,COALESCE(o.quantity,0) old_qty FROM newd n FULL JOIN oldd o ON o.inventory_id=n.inventory_id),chk AS (SELECT COUNT(*) FILTER(WHERE b.new_qty>0)=COUNT(i.id) FILTER(WHERE b.new_qty>0) AND COALESCE(BOOL_AND(CASE WHEN b.new_qty>0 THEN i.id IS NOT NULL AND i.active AND i.category='Abono' AND i.unit='sacos' AND i.farm_id IS NULL ELSE true END),false) AND COALESCE(BOOL_AND(i.current_stock+b.old_qty-b.new_qty>=0),false) AS ok FROM balance b LEFT JOIN public.coffee_inventory i ON i.id=b.inventory_id),upd AS (UPDATE public.coffee_abonadas SET application_date=${x.application_date},farm_id=${x.farm_id},method='Manual',abonada_number=${x.abonada_number},treated_area_ha=${x.treated_area_ha},total_cost=${x.total_cost},responsible=${x.responsible||null},notes=${x.notes||null},updated_by=${a.session.id},updated_at=now() WHERE id=${id} AND (SELECT ok FROM chk) RETURNING id),delprod AS (DELETE FROM public.coffee_abonada_products p USING upd WHERE p.abonada_id=upd.id RETURNING p.id),prod AS (INSERT INTO public.coffee_abonada_products(abonada_id,product_name,quantity,unit,price_per_sack) SELECT upd.id,p.product_name,p.quantity,'sacos',p.price_per_sack FROM upd CROSS JOIN p WHERE (SELECT COUNT(*) FROM delprod)>=0 RETURNING abonada_id),stock AS (UPDATE public.coffee_inventory i SET current_stock=i.current_stock+b.old_qty-b.new_qty,updated_by=${a.session.id},updated_at=now() FROM balance b,upd WHERE i.id=b.inventory_id RETURNING i.id),delmov AS (DELETE FROM public.coffee_inventory_movements m USING upd WHERE m.reference_type='abonada' AND m.reference_id=upd.id AND m.movement_type='salida' RETURNING m.id),mov AS (INSERT INTO public.coffee_inventory_movements(inventory_id,movement_date,movement_type,quantity,unit_cost,farm_id,reference_type,reference_id,notes,created_by) SELECT d.inventory_id,${x.application_date},'salida',d.quantity,i.unit_cost,${x.farm_id},'abonada',upd.id,${`Abonada ${x.abonada_number}`},${a.session.id} FROM newd d JOIN public.coffee_inventory i ON i.id=d.inventory_id CROSS JOIN upd RETURNING id) SELECT (SELECT id FROM upd) id,(SELECT ok FROM chk) inventory_ok`;if(!rows[0]?.inventory_ok||!rows[0]?.id)return NextResponse.json({error:'No se pudo actualizar: verifique inventario general disponible.'},{status:400});return NextResponse.json({ok:true,total_cost:x.total_cost})}catch(e){return dbError(e,'actualizar')}}
